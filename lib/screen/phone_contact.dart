import 'package:flutter/material.dart';

class PhoneContactScreen extends StatelessWidget {
  const PhoneContactScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Phone Contact Screen");
  }
}
